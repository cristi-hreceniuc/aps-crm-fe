<%- include('partials/head', { title: 'Login' }) %>

<section class="login-page">
  <div class="intro-card">
    <img class="login-logo" src="/img/aps-logo.png" alt="Acțiune pentru Sănătate">
    <h1>Acțiune pentru Sănătate – CRM</h1>
    <p>
      Platformă CRM dedicată asociațiilor: gestionează voluntari, donații, cazuri sociale,
      rapoarte și formulare fiscale <strong>(230, 177, sponsorizări)</strong>.
    </p>
  </div>

  <div class="login-card">
    <h2>Autentificare</h2>
    <form id="loginForm" class="form">
      <label>Email</label>
      <input id="email" name="email" type="email" required placeholder="you@example.com" />
      <label>Parolă</label>
      <input id="password" name="password" type="password" required placeholder="••••••••" />
      <button class="btn-primary" type="submit">🔑 Autentificare</button>
    </form>
  </div>
</section>



<script>
document.getElementById('loginForm').addEventListener('submit', async (e) => {
  e.preventDefault();
  const email = document.getElementById('email').value.trim();
  const password = document.getElementById('password').value;

  try {
    const res = await fetch('/login', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ email, password })
    });

    if (res.redirected) {
      window.location.href = res.url;
      return;
    }

    const html = await res.text();
    document.open(); document.write(html); document.close();
  } catch {
    alert('Eroare la conectare');
  }
});
</script>

<%- include('partials/footer') %>
