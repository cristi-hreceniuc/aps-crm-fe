<nav class="nav">
  <div class="nav-inner container">
    <a href="/" class="brand-logo">
      <img src="/img/aps-logo.png" alt="Acțiune pentru Sănătate" />
    </a>
    <div class="nav-links">
      <a href="/">Acasă</a>
      <% if (!isLogged) { %>
        <a href="/login">Autentifică-te</a>
      <% } else { %>
        <form action="/logout" method="post" style="display:inline">
          <button class="btn btn-danger" type="submit">Deconectează-te</button>
        </form>
      <% } %>
    </div>
  </div>
</nav>
